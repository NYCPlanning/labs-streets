import dynamicQueryParam from 'city-map/decorators/dynamic-query-param';
import { module, test } from 'qunit';

module('Unit | Utility | dynamic-query-param', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = dynamicQueryParam();
    assert.ok(result);
  });
});

import precisionRound from 'city-map/utils/precision-round';
import { module, test } from 'qunit';

module('Unit | Utility | precision-round', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = precisionRound();
    assert.ok(result);
  });
});
